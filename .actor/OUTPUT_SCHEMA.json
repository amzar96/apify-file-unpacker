{
    "actorOutputSchemaVersion": 1,
    "title": "File Unpacker Output",
    "description": "Extracted files stored in Key-Value Store with metadata in Dataset",
    "type": "object",
    "properties": {
        "files": {
            "type": "string",
            "title": "Extracted Files",
            "description": "All extracted files with download URLs",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/keys"
        },
        "metadata": {
            "type": "string",
            "title": "File Metadata",
            "description": "File information with download URLs, sizes, and MIME types",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
